"# wucsmsdevsite" 
"# Download and install python"
"# Install Django using the command below"

pip install django

"# Note that pip should come preinstalled after python installation"
"# Install project dependencies using the command below"

"# Download and install wkhtmltopdf,then add its path as a system variable" 

pip install -r requirements.txt

python manage.py migrate

python manage.py runserver


